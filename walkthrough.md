# SSL-Mate (证书伴侣) 功能升级与维护记录

## 一、 优化与功能升级清单

### 1. AuthMate SSO 单点登录对接
- 基于标准 OIDC / OAuth2 + PKCE S256 单点登录协议深度联动；
- 在 OIDC 客户端注册列表中完成客户端注册及重定向回调配置；
- 登录页面提供智能连接诊断与故障引导。

---

### 2. 界面与配置标准化
- 全面标准化企业级文案与标准提示；
- 移除界面所有硬编码本地绝对路径，采用动态配置与环境变量驱动。

---

### 3. 主题模式 (Dark / Light) 切换优化
- Tailwind CSS v4 深度适配 `@custom-variant dark` 类名选择器；
- 增强右上角主题切换按钮交互动效与平滑过渡效果。

---

### 4. 管理员鉴权与安全性升级
- 密码哈希采用 PBKDF2-SHA512 加盐迭代计算；
- 登录页彻底移除明文预填账号密码，提升生产安全等级。

---

### 5. 后台【👥 用户权限管理】模块
- 用户台账管理：头像、用户名、邮箱、认证来源（AuthMate SSO 蓝标 / 本地灾备 灰标）、系统角色（超级管理员/运维操作员/只读审计员）、账号启用/停用状态；
- 用户创建、编辑、角色分配、密码重置；
- 内置防死锁安全防护机制。

---

### 6. 全面适配移动端 (Mobile-Responsive)
- 移动端汉堡导航与毛玻璃滑动抽屉；
- 表格在窄屏设备下自动转化为触摸友好型卡片视图；
- 向导弹窗全端响应式居中。

---

## 二、 运行与验证

```bash
# 启动前端
npm run dev

# 启动后端服务
npm run dev:api
```
